(function () {
'use strict';

function limitFileSize() {
  var pdfForm = document.getElementById("id_pdf");
  var warning = document.getElementById("warning");
  pdfForm.setAttribute("accept", ".pdf");

  pdfForm.onchange = function () {
    var fileSize = pdfForm.files[0].size;

    if (fileSize > 1000000) {
      window.alert("File too big: " + fileSize / 1000 + "kB");
      warning.style.display = "inline";
      $(".mdc-button").each(
      /* @this */
      function () {
        $(this).attr("disabled", true);
      });
    } else {
      warning.style.display = "none";
      $(".mdc-button").each(
      /* @this */
      function () {
        $(this).attr("disabled", false);
      });
    }

    return;
  };
}

limitFileSize();

}());
//# sourceMappingURL=limit.min.js.map
